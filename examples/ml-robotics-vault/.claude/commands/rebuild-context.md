---
description: Regenerate context.md from current wiki contents
---

Run the `/rebuild-context` operation defined in this vault's `CLAUDE.md`.
Snapshot the current `context.md` into `.paperloom/cache/snapshots/`
first, then rewrite it as the current synthesized truth across the vault
(500-2000 words, no citations required) from all contributor entries and
research pages.
