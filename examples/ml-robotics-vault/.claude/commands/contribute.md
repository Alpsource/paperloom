---
description: Process new input into the wiki (a raw paper, a pasted thought, "add this")
argument-hint: [paper id, path under sources/raw/, "all", or a pasted thought]
---

Run the `/contribute` operation defined in this vault's `CLAUDE.md` for:
$ARGUMENTS

Follow that section's workflow exactly — including reading the raw source
and meta.json, searching for existing related pages before creating new
ones, and drafting a plan before writing (unless the input explicitly says
"batch mode", in which case work through everything and show one summary
at the end instead of pausing per item).
